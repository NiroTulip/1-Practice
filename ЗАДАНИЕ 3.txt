
&НаКлиенте
Процедура Команда1(Команда)
	Команда1НаСервере();
КонецПроцедуры

&НаСервере
Процедура Команда1НаСервере()
	Сообщить(ПрибавитьРабочиеДни(Дата("20210808"),3));	
КонецПроцедуры

Функция ПрибавитьРабочиеДни(Знач ИсходнаяДата, Знач Дни)

ТекущаяДата = ИсходнаяДата;

    Счетчик = 0;
	
	Пока Счетчик < Дни Цикл
		
	ТекущаяДата = ТекущаяДата + 86400;
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ
		|	ПроизводственныйКалендарь.Дата,
		|	ПроизводственныйКалендарь.РабочийДень
		|ИЗ
		|	РегистрСведений.ПроизводственныйКалендарь КАК ПроизводственныйКалендарь
		|ГДЕ
		|	ПроизводственныйКалендарь.Дата = &ТекДата";
	
	Запрос.УстановитьПараметр("ТекДата", ТекущаяДата);
	
	РезультатЗапроса = Запрос.Выполнить();
	
	
Если НЕ РезультатЗапроса.Пустой() Тогда
	
	Если РезультатЗапроса.Выгрузить()[0].РабочийДень Тогда
	     Счетчик = Счетчик + 1;  		 
	КонецЕсли;

КонецЕсли;

КонецЦикла;	

Возврат  ТекущаяДата;

КонецФункции // ПрибавитьРабочиеДни()
